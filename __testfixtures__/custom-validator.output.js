import React from "react"

type Props = {
  a?: string,
  b?: unknown,
  c?: unknown,
  d?: unknown,
  e?: unknown,
  f?: unknown,
  g?: unknown
};

export function MyComponent(props: Props) {
  return <span />
}
